# Priority queue

List only the next actionable open tasks, in order, and link each item to its
task file. Keep this list short. Small changes that do not warrant a task file
do not need to appear here.

1. [exomoonlab console interface](exomoonlab-console-interface.md) — a themable
   terminal-desktop console for MoonLab on exotui, running against the native
   library and the WASM build. Start at phase 1, the backend seam.
2. [Browser WebGPU complex64 parity](browser-webgpu-complex64-parity.md) —
   carried over from `ulg`; blocked on backend work that lives on a divergent
   `webgpu` branch.
