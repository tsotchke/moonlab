# Priority queue

List only the next actionable open tasks, in order, and link each item to its
task file. Keep this list short. Small changes that do not warrant a task file
do not need to appear here.

1. [Browser WebGPU complex64 parity](browser-webgpu-complex64-parity.md) —
   currently blocked on backend work that lives on a divergent `webgpu` branch.

The `exomoonlab` console interface is planned on the `exomoonlab` branch; its
task file and plan live there.
