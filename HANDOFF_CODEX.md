# Moonlab v1.2.0 release — codex handoff (2026-07-18)

You are driving Moonlab (C quantum simulator, GitHub tsotchke/moonlab) to a green
ICC release gate and a v1.2.0 tag. Repo: /Users/tyr/Desktop/quantum_simulator.
Current indexed HEAD is `e441957b22698ce93ad4868d585ac7ca3baa281f` on
`master`, with intentional release work still uncommitted. The canonical ICC
task is `moonlab-v1.2.0-release-finalization`; its durable session plan, not an
individual portability task, owns release completion.

## Hard rules (do not violate)
- NEVER add a `Co-Authored-By: Claude` or any AI-attribution trailer to any commit/PR/doc. Stop after the commit body.
- No emojis in any file, commit, or output.
- Commit locally at checkpoints. Do NOT push to origin until the "push" step below (and only if the gate is green).
- Build in isolated FRESH build dirs (prefer `cmake --fresh` and lane-specific
  directories). Stale build dirs produce false failures here — it has bitten
  run_tsan.sh, the numerical/scaling lanes, and the release smoke already.
- Never loosen a tolerance, delete a probe, narrow a corpus, or allowlist a phantom without a located definition, to make a gate pass. Fix the real thing.
- macOS has no `timeout` (use gtimeout or backgrounding). ICC: `ICC_HOME=~/Desktop/infinite_context_coder ~/Desktop/infinite_context_coder/bin/icc <cmd>`.

## The gate (source of truth)
```
ICC_HOME=~/Desktop/infinite_context_coder ~/Desktop/infinite_context_coder/bin/icc \
  readiness --repo moonlab --target moonlab-release-readiness --trace-dir scripts/icc_traces
```
Current saved traces make the oracle read `ready`, score 100, but those traces
were generated on earlier commits (`a58fcb` / `a6f1edb`) and are not sufficient
for the dirty current tree. ICC production-audit is still failing. Every
producer must regenerate its evidence on the exact release tree and the final
certificate must bind commit, source tree, artifacts, mesh, hosted CI, and tag.
Evidence traces live in `scripts/icc_traces/*.jsonl` (gitignored; regenerated by
the producer scripts).

## Historical 8-FAIL handoff and current disposition

1. **hidden_visibility_abi** and **zero_phantom_api** — DONE, on branch
   `worktree-agent-a14a6c94df3050c07`, commit **`92c8c7e`** (51 files). Integrate it:
   ```
   git cherry-pick 92c8c7e     # 50 headers get MOONLAB_API on 429 real (located) defs + .icc/phantom-api-allow.txt (26 justified entries)
   ```
   The "43" was only the linker's fast-fail prefix; the full hidden-vis export
   surface is 429 symbols across tensor/tn/dmrg/tdvp/gpu/metal/crypto/etc plus 22
   Python/Rust FFI symbols. Every tagged symbol is a real located definition (no
   phantoms tagged). Verify:
   - hidden-vis: `rm -rf build-hv && cmake -S . -B build-hv -DCMAKE_BUILD_TYPE=Release -DQSIM_HIDDEN_VISIBILITY=ON -DQSIM_BUILD_TESTS=ON && cmake --build build-hv -j6 && ctest --test-dir build-hv -L "abi|bindings"` -> expect link OK + 6/6.
   - **phantom REQUIRES a fresh ICC index** (this is the caveat): the live `moonlab`
     index is stale and phantom-api will still read ~320 (294 stale false-positives
     from files changed since the last index). After cherry-picking, run
     `ICC_HOME=~/Desktop/infinite_context_coder ~/Desktop/infinite_context_coder/bin/icc index --repo moonlab`
     THEN `icc phantom-api --repo moonlab` -> phantom_count=0 (26 allowed, all with
     cited file:line definitions in .icc/phantom-api-allow.txt; 0 real phantoms).
     The release smoke's `check_phantom` does NOT re-index, so the index refresh must
     happen before you run the smoke or the gate.

2. **full_suite_green** — previously cleared, but must be regenerated from a
   fresh current-tree `-DQSIM_BUILD_TESTS=ON` build. If a test fails, fix the
   regression or stabilize a proven flake; do not rerun until lucky.

3. **deep_hunt_numerical** — the #28 fix and regression are present; regenerate
   `moonlab_numerical.jsonl` on the exact release tree. Any failure is a real
   numerical finding, not plumbing.

4. **deep_hunt_scaling** — the stale free-pass was removed and the quarantine is
   empty; regenerate the full owning harness on the exact release tree. Do not
   reintroduce a bypass or empty a quarantine without a passing harness.

5. **mpi_sharded_gpu_works** — an exact fleet proof exists for commit `a58fcb`
   (`N=33`, four ranks, two hosts, two GPU endpoints, halo swaps), but it must be
   regenerated or cryptographically bound to the final release commit. Do not
   substitute a local fake for the hardware lane.

6. **ci_all_green** — needs the branch pushed so GitHub CI runs. This is the PUSH step; do it only after everything else is green (see below).

7. **failure_free** — derivative: it clears only when freshly regenerated
   current-tree traces contain no real or relay-generated FAIL event. Revalidate
   the deep-hunt relay while regenerating the release smoke.

## Current release campaign additions

- Complete the Linux/Windows wheel-builder task and prove warnings, package
  install, CMake consumer, pkg-config consumer, and OpenMP linkage without
  weakening tests.
- Keep the canonical ten-profile amd64/arm64 Linux evidence matrix at zero
  skips, plus the additional distro-family smoke lanes.
- Clear ICC production-audit, including the CMake modularity blocker and
  release-script hardening findings, before final signoff.
- Regenerate release smoke, ABI/bindings/docs/examples/sanitizers, oracle, fuzz,
  differential, statistical, TSan, numerical, scaling, MPI, and 5/5 mesh proof
  on the exact release tree.
- Commit locally at reviewable checkpoints. History rewriting, pushing, and
  publishing `v1.2.0` remain explicit approval boundaries.

## Final sequence (each mutating boundary requires explicit approval)

The tag/artifact cycle is resolved by `release.yml`'s non-publishing manual
candidate mode and tag-triggered exact-byte promotion. STOP before tag push:
`publication-readiness` deliberately fails until the exact certified `.crate`
bytes can be uploaded through an audited Cargo/crates.io protocol path. Do not
remove that stop or permit any registry mutation as a workaround.

1. Finish reviewable local checkpoint commits, then obtain approval to scrub
   every prohibited attribution trailer. Rewrite the 11 inventoried commits,
   verify `git log --all --grep "Co-Authored-By"` is empty, and make the final
   clean release commit on the rewritten history.
2. Regenerate all local evidence on that exact clean tree. Evidence made before
   the rewrite is diagnostic unless the certificate validator proves identical
   tree/fingerprint continuity.
3. Obtain approval to push `master` with `--force-with-lease`. Run the manual
   `Release` workflow for version `1.2.0` on that exact head. It must remain
   non-publishing and produce a successful, complete hosted matrix plus the
   sealed 22-artifact candidate manifest. Regenerate the exact N=33 MPI proof
   and named five-node mesh proof against the same clean source identity.
4. Obtain approval to create the local annotated `v1.2.0` tag. Its annotation
   contains exactly `Moonlab-Release-Candidate-Run: <run_id>` and
   `Moonlab-Release-Candidate-Head: <40-hex>` on separate lines. Generate the
   external, untracked machine-readable certificate under
   `scripts/icc_traces/release-certificate/`; validate its commit/tree/source,
   lane, exact candidate artifacts, MPI, mesh, hosted-run, and annotated-tag
   bindings.
5. Only after the full ICC oracle and external certificate both pass, and the
   exact-byte crates.io publisher is implemented and verified, obtain approval
   to push `v1.2.0`. Promotion must reuse the tag-bound candidate bytes and
   publish nothing if any check differs. This tag becomes the pin for Eshkol PR
   #299 (task #26 — it needs `vqe_compute_qgt` exposed, which is already
   exported).

## Context you may need
- Prior campaign work (all landed + verified on master): reversed-CNOT, interior-norm (S1), Metal float32 fp-floor (exact 2q on CPU, lossy Metal opt-in via MOONLAB_TN_GPU_LOSSY), 5 concurrency races + audit deadlock, control-plane 64GB DoS (control_max_qubits cap), SVD-fallback isometry (#28), wheel -Werror (-iframework). See .icc/BUG_CLOSURE_CAMPAIGN.md.
- Mesh Windows (win11 guest on old-donkey) is BLOCKED on an ssh authorized_key and is additive — Windows is already green on GitHub CI. Do not spend effort on it.
- Report progress by re-running the gate. `ready` is necessary but release
  completion additionally requires the valid external certificate and approved
  tag publication.
