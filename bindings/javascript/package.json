{
  "name": "moonlab-javascript",
  "version": "1.2.0",
  "private": true,
  "description": "Moonlab Quantum Simulator - JavaScript/WebAssembly Bindings",
  "scripts": {
    "build": "turbo run build --filter=!@tsotchkecorp/moonlab-demo",
    "build:wasm": "pnpm --filter @tsotchkecorp/moonlab build:wasm",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:unit": "pnpm --filter @tsotchkecorp/moonlab test:unit",
    "test:integration": "pnpm --filter @tsotchkecorp/moonlab test:integration",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
