# git-cliff configuration for MoonLab Quantum Simulator
# https://git-cliff.org/docs/configuration

[changelog]
# Header of the changelog
header = """
# Changelog

All notable changes to MoonLab Quantum Simulator will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

"""

# Template for the changelog body
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}**{{ commit.scope }}**: {% endif %}\
            {{ commit.message | upper_first }}\
            {% if commit.breaking %} [**BREAKING**]{% endif %}\
    {% endfor %}
{% endfor %}\n
"""

# Footer of the changelog
footer = """
---
Generated by [git-cliff](https://git-cliff.org/)
"""

# Remove leading and trailing whitespace
trim = true

[git]
# Parse conventional commits
conventional_commits = true

# Filter commits by patterns
filter_unconventional = true

# Process commits that match the regex
split_commits = false

# Regex for commits to include
commit_preprocessors = [
    { pattern = '\((\w+)\)!:', replace = "($1):" },
]

# Regex for parsing commits
commit_parsers = [
    { message = "^feat", group = "✨ Features" },
    { message = "^fix", group = "🐛 Bug Fixes" },
    { message = "^doc", group = "📚 Documentation" },
    { message = "^perf", group = "⚡ Performance" },
    { message = "^refactor", group = "🔧 Refactoring" },
    { message = "^style", group = "🎨 Styling" },
    { message = "^test", group = "🧪 Testing" },
    { message = "^chore\\(release\\)", skip = true },
    { message = "^chore\\(deps.*\\)", skip = true },
    { message = "^chore\\(pr\\)", skip = true },
    { message = "^chore\\(pull\\)", skip = true },
    { message = "^chore|^ci", group = "⚙️ Miscellaneous" },
    { message = "^security", group = "🔒 Security" },
    { body = ".*security", group = "🔒 Security" },
    { message = "^revert", group = "⏪ Reverted" },
]

# Protect breaking changes from being skipped
protect_breaking_commits = true

# Filter out commits by their regex
filter_commits = false

# Tag pattern regex
tag_pattern = "v[0-9].*"

# Skip tags regex
skip_tags = ""

# Ignore tags regex
ignore_tags = ""

# Sort commits by date or by number of patches
topo_order = false

# Sort order for commits (oldest or newest first)
sort_commits = "oldest"

# Limit the number of commits to include
# limit_commits = 42
